import type { CSSProperties } from 'react';

/** A `<button>` that reads as a text link (the df-link class only sets the colour). */
export const linkButtonStyle: CSSProperties = {
  background: 'none',
  border: 0,
  padding: 0,
  cursor: 'pointer',
  font: 'inherit',
  fontSize: 11.5,
};
